let collectSound = AudioFX('Sounds/collect', { formats: ['mp3'], pool: 12 });
let landSound = AudioFX('Sounds/land', { formats: ['mp3'], pool: 1 });
let jumpSound = AudioFX('Sounds/jump', { formats: ['mp3', 'ogg', 'wav'], pool: 1 });
let startFallSound = AudioFX('Sounds/startFall', { formats: ['mp3'], pool: 1 });
let continueFallSound = AudioFX('Sounds/continueFall', { formats: ['mp3'], pool: 1 });
let endFallSound = AudioFX('Sounds/endFall', { formats: ['mp3'], pool: 1 });
let fallSound = AudioFX('Sounds/fall', { formats: ['mp3'], pool: 1 });